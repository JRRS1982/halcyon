# Enforce the Node version declared in package.json `engines` (sourced from
# .nvmrc). Without this, a mismatched Node only warns; with it, install fails
# fast — so local, CI, and Vercel all run the same Node line.
engine-strict=true
