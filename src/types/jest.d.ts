// Pull in @testing-library/jest-dom's jest.Matchers augmentations so that
// `toBeInTheDocument`, `toBeDisabled`, etc. type-check in *.test.tsx files
// without needing a per-file import.
/// <reference types="@testing-library/jest-dom" />
